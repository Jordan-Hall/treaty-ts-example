import { signal, input } from '@angular/core';

<style>
  h1 {
		background-color: black;
		color: white;
	}
</style>
const counter = signal(0);
const label = input('Counter');
<h1>{{ label }} {{ counter() }}</h1>


function increase() {
	counter.update(v => v + 1);
}

<button (click)="increase()"> Increase value </button>